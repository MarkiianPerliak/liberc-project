{
  "name": "prc",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx parcel src/index.html",
    "build": "parcel build src/index.html --no cache --public-url //liberc-project/"
  },
  "author": "Markiisn",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "parcel": "^2.14.4",
    "parcel-reporter-clean-dist": "^1.0.4"
  }
}
